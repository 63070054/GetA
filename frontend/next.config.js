/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  server: {
    // host: "localhost",
    host: "3.86.227.129",
    port: 3000,
  },
};

module.exports = nextConfig;
